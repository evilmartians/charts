{{- range .Values.apps }}
{{- if .storage.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .name }}-cloudsql-backup-create
spec:
  {{toYaml .storage.spec | nindent 2 }}
{{- end }}
{{- end }}